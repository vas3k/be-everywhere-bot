[project]
name = "be-everywhere-bot"
version = "0.2.0"
description = "Mesh-sync posts across social networks (Twitter, Telegram, Mastodon)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "sqlalchemy>=2.0.51",
]
